# Test-only dependencies.
pytest
pytest-asyncio
aioresponses
# HA test harness for tests/resideo/ (pins the matching homeassistant version itself,
# which satisfies the unpinned `homeassistant` in requirements.txt).
pytest-homeassistant-custom-component
